services:
  backend:
    build:
      context: ./backend
    container_name: no_snoring_backend
    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env
      
    environment:
      - MONGO_URI=mongodb+srv://zzcare_admin:Zzcare2026Secure@cluster0.zjfudsg.mongodb.net/?appName=Cluster0
      - MONGO_DB=zzcare_db
    restart: always
